<h1> <%= @recipe.title %> <h1> 
<p> Creator: <%= link_to @recipe.creator.name, user_path(@recipe.creator.id)%></p>
<h3> Ingredients: <% @recipe.recipe_ingredients.each do |i| %> </h3>
   <li> <%= i.measurement %> <%= link_to i.ingredient.name, ingredient_path(i.ingredient_id) %> </li> 
   <%end%> 



